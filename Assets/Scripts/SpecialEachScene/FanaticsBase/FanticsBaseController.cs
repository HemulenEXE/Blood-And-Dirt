using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Отвечает за активацию/деактивацию нужных скриптов при каждом новом попадании на эту сцену
public class FanticsBaseController : MonoBehaviour
{
    public int LoadingNumber; //Фиксирует число заходов на эту сцену, т.к. каждый раз на ней происходят разные действия
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
